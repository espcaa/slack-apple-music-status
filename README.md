# apple music thingies

only macos now :scheming:

## setup

1. if you trust me, run the install script

`sh install.sh`

if you ever need to update it, just run
`sh install.sh update`

## things

everything's logged in `~/music.log`, you might need to delete it if it gets too big

## idk

format of the log file is

```
timestamp|ACTION|prevName|prevArtist|prevAlbum|currName|currArtist|currAlbum
```
